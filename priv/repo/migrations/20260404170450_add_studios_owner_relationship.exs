defmodule Loka.Repo.Migrations.AddStudiosOwnerRelationship do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:users) do
      add :studio_id,
          references(:studios,
            column: :id,
            name: "users_studio_id_fkey",
            type: :uuid,
            prefix: "public"
          )
    end
  end

  def down do
    drop constraint(:users, "users_studio_id_fkey")

    alter table(:users) do
      remove :studio_id
    end
  end
end
